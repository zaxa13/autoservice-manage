[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra --strict-markers --strict-config
markers =
    slow: tests, выполняющиеся дольше 1с
